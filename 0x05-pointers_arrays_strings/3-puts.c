#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _puts -  prints a string, followed by a new line
 * @str: points to string
 * Return: string and a new line
 */

void _puts(char *str)
{
	puts(str);
}
