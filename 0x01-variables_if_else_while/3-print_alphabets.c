#include<stdio.h>
/**
 * main - Entry point
 * Return: 0 (success)
 */

int main(void)
{
	char n;
	char f;

	n = 'a';
	f = 'A';
	while
		(n <= 'z') {
			putchar(n);
			n++;
		}
	while
		(f <= 'Z') {
			putchar(f);
			f++;
		}
	putchar ('\n');
	return (0);
}
